## 📘 วิธีสร้าง Virtual Environment ของ Python (env)
#### ก่อนสร้างพื้นที่ในการเก็บส่วนเสริมของ Python เราจะต้องสร้างโฟรเดอร์เพื่อจัดเก็บ env และ โปรเจ็คของเรา โดยให้ใช้คำสั่งตามด้านล่างต่อไปนี้

### 1️. สร้างโฟรเดอร์ และ เข้าถึงโฟรเดอร์ที่เราสร้าง
```bash
mkdir my_project
cd my_project
```
### 2. การสร้าง Virtual Environment ใน Windown และ macOS/Linux
#### 2.1. สร้าง Virtual Environment (Windows)
```bash
python -m venv env
```
#### 2.2. สร้าง Virtual Environment macOS/Linux
```bash
python3 -m venv env
```
---
## 📘 เข้าถึง Virtual Environment (Activate Virtual Environment)
#### เมื่อสร้าง Virtual Environment เสร็จแล้วถ้าเราจะเข้าถึง หรือเรียกใช้งาน Virtual Environment ให้ใช้คำสั่งตามด้านล่างต่อไปนี้
### 1️. เข้าถึง Virtual Environment ใน windows
```bash
env\Scripts\activate
```
### 2. เข้าถึง Virtual Environment ใน macOS/Linux
```bash
source env/bin/activate
```
### 3. ผลลัพธ์เมื่อเข้าถึง Virtual Environment สำเร็จ
#### 3.1. เมื่อเข้าถึง Virtual Environment ใน windows สำเร็จ 
```bash
(env) PS C:Users\playmaker\Downloads/my_project>
```
#### 3.2. เมื่อเข้าถึง Virtual Environment ใน macOS/Linux สำเร็จ 
```bash
(env) playmaker@playmaker\Downloads my_project %
```
---
## 📘 ออกจาก Virtual Environment (deactivate)
#### ถ้าหากต้องการจะออกจาก Virtual Environment ให้ใช้คำสั่งตามด้านล่างต่อไปนี้
```bash
deactivate
```
---
## 📘 ติดตั้งส่วนเสริมจากไฟล์ requirements.txt
#### requirements.txt  คือ ไฟล์ที่เก็บแพ็คเกจและไลบรารี่ต่าง ๆ ของไพธอนโปรเจคท์ของเรา ซึ่งช่วยให้เราเรียกใช้ packages หรือ libraries ในโปรเจคท์เดิมของเราได้อย่างสะดวก (อ้างอิง https://devhub.in.th/learn/python/requirements-txt)
#### การติดตั้งส่วนเสริมในไฟล์ requirements txt จะใช้คำสั่งต่อไปนี้
```bash
#### -r ย่อมากจาก read ใช้ในการอ่านไฟล์ที่เรากำหนด
pip install -r requirements.txt 
```
---
## 📘 วิธีอัปเดตโปรเจคขึ้น GitHub
#### เมื่อมีการแก้ไขไฟล์ในโปรเจคของเรา เวลาทำการอัพเดตขึ้น github ให้ใช้คำสั่งตามด้านล่าง

### 1️. ดึงไฟล์ในโฟลเดอร์ทั้งหมด เพื่อเตรียมไฟล์สำหรับการ Commit
```bash
git add .
```

### 2. สร้างโน๊ตสำหรับเปลี่ยนแปลงของไฟล์ที่ดึงมาทั้งหมด
```bash
git commit -m "ข้อความที่จะให้บันทึกสำหรับการอัพเดต"
```

### 3. ดึงไฟล์จากการ commit ทั้งหมดอัพเดตขึ้น github
```bash
git push
```
---